import React from 'react';

const Pie = ({ contenido }) => {
    return (
        <footer className="py-3 bg-light text-center">
            {contenido}
        </footer>
    );
};

export default Pie;